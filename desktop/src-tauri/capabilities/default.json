{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local desktop workspace shell.",
  "windows": ["main", "voice"],
  "permissions": []
}
